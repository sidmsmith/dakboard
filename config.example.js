// Example local configuration file
// Copy this to config.js and fill in your values for local development
// config.js is gitignored and will not be committed

window.CONFIG = {
  // Local development mode - set to true to use direct HA API calls
  LOCAL_MODE: true,
  
  // Your Home Assistant URL (for local development)
  HA_URL: 'http://sidmsmith.zapto.org:8123', // Update with your HA URL
  
  // Your Home Assistant long-lived access token (for local development)
  HA_TOKEN: 'your_long_lived_access_token_here', // Update with your token
};



